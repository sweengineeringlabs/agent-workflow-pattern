//! `agent-workflow-pattern` — the Workflow pattern for agents.
//!
//! Defines the contract for agent workflow specification.

#![deny(unsafe_code)]
#![warn(missing_docs)]

mod traits;

pub use traits::*;
